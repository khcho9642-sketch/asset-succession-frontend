# Server only. Never use NEXT_PUBLIC_ for these values.
# No key is needed for the explicitly labelled guided intake/report flow.
AI_GATEWAY_API_KEY=
# Set a provider/model identifier supported by the existing AI Gateway account.
AI_DIAGNOSIS_MODEL=
